export default function MiniLine({values=[],height=90,label}){const a=values.map(Number).filter(Number.isFinite);if(a.length<2)return null;const min=Math.min(...a),max=Math.max(...a),span=max-min||1,w=500,p=6,pts=a.map((v,i)=>`${p+i*(w-2*p)/(a.length-1)},${p+(max-v)/span*(height-2*p)}`).join(' ');return <div><svg viewBox={`0 0 ${w} ${height}`} preserveAspectRatio="none" style={{display:'block',width:'100%',height}}><line x1="0" y1={height-1} x2={w} y2={height-1} stroke="#ecece7"/><polyline points={pts} fill="none" stroke="#202226" strokeWidth="2" vectorEffect="non-scaling-stroke"/></svg>{label&&<div className="muted" style={{fontSize:10}}>{label}</div>}</div>}
